import { describe, it, expect } from 'vitest';
import ScanTerminalStream from '../../components/ScanTerminalStream';

describe('ScanTerminalStream component', () => {
  it('is defined and is a valid React component function', () => {
    expect(typeof ScanTerminalStream).toBe('function');
  });
});
